import { SlashCommandPartial } from 'deps';

export const commands: SlashCommandPartial[] = [
  {
    name: 'setup',
    description: 'Sets the current channel as the Q&A target',
    options: []
  }
];
